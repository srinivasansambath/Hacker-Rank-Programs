#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
	
    int n,i,temp=0,sum=0;
    scanf("%d", &n);
    for(i=0;i<5;i=i+1)
    {
        temp = n%10;
        sum = temp + sum;
        n = n/10;
    }
    printf("%d",sum);
    return 0;
}

OUTPUT:
10564
16

